# 2026-07-13 移动端视觉回归验收

## 范围

本次验证在独立临时配置下运行：服务只监听 `127.0.0.1:18448`，使用隔离数据目录和默认测试账号，不读取项目中的真实凭据或现有用户数据。

验证视口为 390px 宽度，覆盖此前 2026-07-10 验收记录中的三个 P2 UI 项。

## 结果

| 项目 | 结果 | 证据 |
|---|---|---|
| 登录页辅助文字对比度 | ✅ 通过 | `.login-tagline` 为 `rgba(255,255,255,0.62)`，`.login-footer` 为 `rgba(255,255,255,0.5)`；页面无横向溢出。 |
| 主查询页窄屏布局 | ✅ 通过 | 文档根元素 `scrollWidth` 与 `clientWidth` 相等；不再出现页面级横向滚动。 |
| 定时任务页窄屏布局 | ✅ 通过 | 页面根元素无横向溢出；任务表放入 `.scheduler-table-scroll`，表宽 760px 时在容器内横向滚动，不再压缩表头或撑宽页面。 |

## 同步的接口改进

巡检历史底层已具备 SQL `LIMIT/OFFSET` 分页。本轮将 `GET /api/v1/tamper/history` 的 `offset` 参数公开，并限制在 `0..100000`；新增回归测试确认 `limit=1&offset=1` 返回第二新记录。

## 未覆盖范围

真实外部搜索引擎查询、已登录页面截图和扩展采集会消耗账户配额或使用部署凭据。本次没有执行这些外部动作；上线前应使用已授权、最小权限的凭据另行完成成功态验收。
