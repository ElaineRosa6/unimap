# Audit fix record — 2026-08-31

This change was made on the existing dirty working tree; unrelated edits were preserved.

## Confirmed issue and change

Several container-only diagnostic scripts use pytest-compatible filenames and execute live filesystem/network actions at import time. `smtp-relay/pytest.ini` now restricts unit discovery to the actual offline environment-loading test.

## Verification

- `python -m pytest -q` from `smtp-relay` — 3 passed, exit 0
- `node --test test/*.test.mjs` from `tools/extension-screenshot` — 32 passed, exit 0
